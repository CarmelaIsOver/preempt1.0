# preempt1.0：面向大模型提示词训练数据的多层级隐私脱敏系统

**作者邮箱**：carmela@example.com
**提交日期**：2026.05.27

---

## 摘要

大语言模型（LLM）的规模化训练依赖于海量用户交互数据。在典型的训练数据采集流程中，服务商收集的是用户提交的原始提示词（prompt）——而非完整的问答对。这些提示词常包含个人身份信息、敏感数值和领域专有实体，在进入模型训练管线前需要经过隐私清洗。传统脱敏方案——规则匹配、占位符替换或简单泛化——在直接应用于提示词预处理时，会系统性地破坏数据效用：数值约束关系断裂导致模型学习错误推理模式，医疗等专业实体的语义失真导致领域知识退化。更关键的是，占位符替换（如"[PERSON]"、"[AGE]"）使脱敏数据与真实分布产生结构性偏差，模型无法习得正确的命名实体语境分布。

近年来，训练数据提取攻击和成员推断攻击的快速发展，使得训练数据隐私面临前所未有的威胁。Carlini 等人在 USENIX Security 2021 上系统性地证明，攻击者可通过精心设计的提示词从 GPT-2 等模型中逐字提取训练数据中的个人身份信息，该研究荣获杰出论文奖，从根本上改变了学术界对 LLM 隐私风险的认知。随后，针对 RAG 架构的指令劫持攻击、基于软提示的自动化提取攻击 CoSPED 等方法相继涌现，形成了对大模型训练数据的系统性威胁体系。

本文提出 preempt1.0——一个面向大模型训练数据的多层级隐私脱敏系统。系统构建了三标签（T1/T2/T3）实体分类与差异化脱敏框架：（1）T1 个人身份信息：PERSON 实体采用基于确定性哈希的跨样本一致性假名替换，保证同一实体在所有训练样本中映射到相同假名；其余实体采用基于 Unicode Block 自适应的 FF3 格式保持加密，密文格式与明文一致，不破坏模型的分词与格式感知能力；（2）T2 数值型实体：采用自适应对称域指数机制的局部差分隐私扰动，并通过有向无环图保持关联数值间的数学约束关系；（3）T3 领域实体：结合 Sentence-Transformer 语义向量空间，通过类比推理（`v_child - v_root_orig + v_root_pert`）保持疾病-药物-症状的方向一致性，利用指数机制实现语义保留且可量化的差分扰动。

为验证系统的安全防御能力，本文设计了前缀注入攻击和成员推断攻击两种威胁模型的消融实验，基于 Qwen2.5-0.5B + LoRA 微调的 15 个模型（5 消融层级 × 3 随机种子）进行安全性验证。实验结果表明：（1）经 preempt1.0 全链路脱敏后训练的模型，前缀注入攻击的 PII 提取率从 100% 降至 0%；（2）消融实验证实 T1 层（姓名+FF3）贡献了主要防御能力（↓96.7%），T2 层进一步增强至 100%。在数据效用方面，实体识别 F1 值达 91.3%；T1 实体跨样本假名一致性 100%；T2 数值扰动在 ε=1.0 时均值相对误差 2.8%，DAG 约束满足率 99.6%；T3 领域实体采用真实 SentenceTransformer 编码，ST 语义相似度 0.74，领域一致性 65.4%（M3E 中文编码器实测 68.3%，留作未来工作进一步提升）。系统支持可替换 NER 后端，本地规则引擎处理延迟 0.5ms/条，云端 API 约 400ms/条。

**关键词**：训练数据脱敏 差分隐私 格式保持加密 命名实体识别 训练数据提取攻击 成员推断攻击 大语言模型

---

## 目录

- 第一章 引言
- 第二章 背景与相关工作
  - 2.1 训练数据隐私威胁
  - 2.2 差分隐私基础
  - 2.3 格式保持加密
  - 2.4 相关工作
- 第三章 威胁模型与安全需求
  - 3.1 训练数据提取攻击
  - 3.2 成员推断攻击
  - 3.3 安全需求定义
- 第四章 系统设计
  - 4.1 设计原则与系统概述
  - 4.2 实体识别层
  - 4.3 T1 身份信息脱敏
  - 4.4 T2 数值信息脱敏
  - 4.5 T3 领域实体脱敏
  - 4.6 批处理与数据输出
- 第五章 核心算法
  - 5.1 自适应 Unicode Block FF3 加密
  - 5.2 自适应对称域 mLDP 机制
  - 5.3 DAG 拓扑约束传播
  - 5.4 ST+FT 语义指数机制
- 第六章 实验评估
  - 6.1 实验设置
  - 6.2 实体识别效果
  - 6.3 隐私-效用权衡
  - 6.4 脱敏数据质量评估
  - 6.5 安全性验证实验
  - 6.6 性能与可扩展性
  - 6.7 与基线方法对比
- 第七章 讨论
  - 7.1 训练数据脱敏 vs 实时脱敏
  - 7.2 ε 参数选择与隐私论证
  - 7.3 局限性与未来工作
- 参考文献

---

## 第一章 引言

大语言模型的性能提升高度依赖于训练数据的规模与质量。OpenAI、Anthropic、Google 等主流模型提供商均通过收集用户提示词（prompt）来持续优化模型能力[1]。需要特别指出的是，当前主流的训练数据采集范式以**用户提示词**为核心——服务商收集的是用户输入的原始文本，而非完整的对话对或模型响应。这种"仅提示词"（prompt-only）的训练模式意味着隐私保护的重点聚焦于用户提交的文本内容。

然而，用户提示词中常包含姓名、联系方式、地址等个人身份信息（PII），以及医疗诊断、金融交易、法律咨询等高度敏感的领域实体。这些提示词在进入训练管线前若未经过充分的隐私处理，将带来多重风险：

**第一，模型记忆与训练数据提取风险**。Carlini 等人[2]在 USENIX Security 2021 系统性地证明，大语言模型在训练过程中会逐字记忆训练数据，攻击者可通过精心构造的提示词从模型中提取出包括姓名、电话号码、电子邮箱在内的完整个人身份信息。该研究提取了数百个逐字记忆的训练文本片段，并发现更大的模型倾向于记忆更多训练数据。这一发现彻底改变了学术界和工业界对 LLM 隐私风险的认知——隐私泄露不再是理论假设，而是已有实证的严重威胁。随后，针对检索增强生成（RAG）架构的指令劫持攻击[3]、基于软提示的自动化定向提取攻击 CoSPED[4]等方法相继涌现，将训练数据提取的威胁面从直接生成扩展到了间接诱导和自动化探测。

**第二，成员推断攻击风险**。Shokri 等人[5]在 IEEE S&P 2017 首次提出了针对机器学习模型的成员推断攻击（Membership Inference Attack, MIA），其核心思想是：给定一条数据记录和一个目标模型，判断该记录是否存在于模型的训练集中。对于医疗等敏感领域，仅"某人患有某病"这一训练集从属关系本身就构成隐私泄露。Carlini 等人[6]进一步将成员推断攻击与大语言模型结合，证明了即使是最先进的 LLM 也难以免疫此类攻击。近期研究表明，针对 RAG 系统的成员推断攻击仅需 5 次查询即可达到高准确率[7]。

**第三，合规风险**。全球范围内的数据保护法规对个人信息的处理提出了严格要求。中国《个人信息保护法》第 13 条要求个人信息处理具有明确目的、限于必要范围[8]；欧盟 GDPR 第 5 条规定数据最小化原则[9]；美国 HIPAA Safe Harbor 规则要求在数据去标识化后才能用于研究等二次利用场景[10]。将包含 PII 的原始提示词直接用于模型训练，可能违反上述法规，面临高额罚款和监管处罚。

**第四，传统脱敏方案严重损害训练数据效用**。当前业界常用的脱敏方法存在系统性缺陷：

- **基于规则匹配的占位符替换**（如将"张三"替换为"[PERSON]"）虽然简单高效，但引入了结构性偏差。训练数据中大量出现"[PERSON]"、"[AGE]"等人造 token，破坏了自然语言分布，模型无法学习真实命名实体在语境中的共现模式和分布规律。
- **数值的简单泛化**（如将精确年龄归入区间）破坏数值间的数学约束关系。例如"单价 150 元×数量 3=总价 450 元"经过独立泛化后，可能出现"单价 100-200 元×数量 3≠总价 400-500 元"的逻辑矛盾，导致模型学习到错误的数值推理模式。
- **领域实体的随机替换或删除**直接损害模型在专业领域的知识表示能力。在医疗领域，将"肺炎"替换为不相关的词汇或直接删除，会破坏疾病-症状-药物的关联知识图谱，模型在此类领域的推理能力将严重退化。

上述问题揭示了当前 LLM 训练数据预处理中的一个核心矛盾：**如何在有效保护个体隐私的前提下，最大限度地保留训练数据的统计效用与语义完整性？**

针对这一问题，本文提出 preempt1.0——一个面向大模型提示词训练数据的多层级隐私脱敏系统。系统的核心设计理念是：**"脱敏是提示词进入训练管线的预处理工序，而非对模型响应的临时遮蔽"**。本系统聚焦于 prompt-only 训练范式下的用户输入侧隐私保护，在提示词被用于模型训练之前完成隐私清洗，使得即使攻击者能够从训练后的模型中提取训练样本，所能获得的也只是脱敏后的假名、密文和扰动值。基于这一理念，我们在以下方面进行了针对性的设计：

1. **基于真实攻击的威胁建模与防御验证**：系统设计从训练数据提取攻击和成员推断攻击两大真实威胁出发，明确定义了各层级的防御目标。在实验评估中，通过模拟前缀注入攻击和成员推断攻击，定量验证了系统的防御有效性。

2. **跨样本一致性的假名体系**：不同于实时脱敏场景下需要跨会话隔离以防止关联攻击，训练数据脱敏需要同一实体在**所有样本中映射到相同的假名**。本系统通过确定性哈希（实体原文→固定种子）实现跨样本一致的假名替换，确保模型能够在大量样本中习得稳定的实体指代关系。

3. **格式保持而非占位符**：对所有非人名的 T1 实体采用 FF3 格式保持加密，密文保持原始数据的字符集和长度特征（如手机号仍是 11 位数字、身份证号仍是 18 位）。模型可基于格式信息进行推理，训练数据的 token 分布不被人造占位符污染。

4. **可证明的差分隐私保证**：对数值和领域实体采用基于指数机制的局部差分隐私扰动，提供数学上可证明的隐私保护强度（ε 参数）。这一形式化保证使得脱敏数据满足差分隐私的定义，从机制层面抵御成员推断攻击和模型逆向攻击。在对 T3 领域实体的处理上，我们选择了 指数机制+类比推理 而非 LLM 直接替换，核心原因在于前者提供了可量化的隐私保护强度证明，而后者作为黑盒生成系统无法提供任何形式化的安全保证。

5. **约束保持的数值扰动**：创新性地将数值实体间的依赖关系建模为有向无环图（DAG），通过拓扑排序和根节点优先扰动策略，保证关联数值扰动后仍严格满足原始数学关系。该设计解决了数值脱敏领域长期存在的"约束保持"难题。

本文后续章节组织如下：第二章介绍背景知识与相关工作；第三章详述威胁模型；第四章阐述系统设计；第五章深入分析核心算法；第六章通过实验验证系统有效性；第七章总结并讨论未来工作。

---

## 第二章 背景与相关工作

### 2.1 训练数据隐私威胁

大语言模型的训练数据隐私风险已从理论探讨发展为实证威胁。本节系统性地梳理该领域的关键研究成果。

#### 2.1.1 训练数据记忆化现象

训练数据记忆化（memorization）是指模型在参数中以某种形式"存储"了训练样本的完整信息。Carlini 等人[2]首次对 GPT-2 系列模型进行了大规模记忆化量化研究，核心发现包括：

- 从 6 亿参数的 GPT-2 中成功提取了数百条逐字记忆的训练文本，包括完整的姓名、电话号码、电子邮箱地址和 UUID
- 模型规模与记忆化程度正相关：15 亿参数的 GPT-2 XL 比 1.24 亿参数的 GPT-2 Small 记忆了更多训练数据
- 即使训练数据仅出现一次（而非多次重复），仍可能被模型记忆
- 记忆化倾向于发生在"异常值"文本上——即那些与训练数据整体分布显著不同的罕见片段

这些发现的确切意义在于：**大语言模型的训练数据隐私泄露不是概率性风险，而是确定性事实**。

随后，Carlini 等人[11]进一步扩展了研究范围，发现记忆化现象不仅存在于 GPT-2，在 GPT-Neo、LLaMA 等多种架构上均存在。他们提出了"反绎式记忆化"的概念：一条数据被记忆，当且仅当模型在见到该数据和不见到该数据的训练结果有显著差异。

#### 2.1.2 训练数据提取攻击

训练数据提取攻击是最直接的威胁形式：攻击者通过构造查询，直接从模型输出中获取训练数据片段。

**前缀注入攻击**是最经典的提取攻击形式[2]。攻击原理是：攻击者获取某文本的公开前缀（如社交媒体的公开个人信息片段），将其输入模型作为提示词，令模型完成该文本的后续内容。如果模型在训练中记忆了该完整文本，则会逐字输出后缀部分。

具体流程如下：

1. **候选生成**：从互联网公开数据中收集文本前缀
2. **模型查询**：以前缀为 prompt，请求模型完成后续内容
3. **输出筛选**：设计困惑度等指标筛选候选输出，识别最可能属于训练数据的片段
4. **隐私提取**：从筛选出的输出中提取个人身份信息等敏感内容

这种攻击之所以有效，核心在于大语言模型的"语言建模"本质——模型被训练为给定前缀，预测下一个 token。当训练数据被记忆时，模型对记忆片段的预测置信度异常高，近乎"复述"。

**针对 RAG 的攻击**是更近期的威胁演进。RAG 架构在大模型应用中广泛使用——模型在生成回答前先从知识库中检索相关文档。攻击者可注入对抗性指令，绕过系统的安全约束，令模型直接输出检索库中的原始文档[3]。这类攻击利用的是 RAG 架构中检索和生成两个环节的耦合脆弱性。

**软提示提取攻击**是自动化程度最高的方法。Zhou 等人提出的 CoSPED[4]攻击框架，通过训练一个软提示向量（而非手工设计文本 prompt），自动最大化模型对训练数据的输出概率。实验表明，这种自动化方法可以将训练数据提取率提升至 65.2%。

#### 2.1.3 成员推断攻击

成员推断攻击由 Shokri 等人[5]在 IEEE S&P 2017 首次系统提出。其攻击目标是判断某条特定数据记录是否属于目标模型的训练集。

**基本原理**：机器学习模型对训练样本的预测通常比非训练样本更"自信"（更高的置信度、更低的损失值）。攻击者利用这一差异构建二分类器，区分训练集成员与非成员。

**针对语言模型的 MIA**：Carlini 等人[6]将 MIA 框架应用于大语言模型，提出了基于困惑度（perplexity）的成员推断方法。核心假设是：对于一条文本，如果模型在其上的困惑度显著低于在类似文本上的基准困惑度，则该文本很可能属于训练集。该方法在多个 LLM 上验证有效，AUC 超过 0.75。

**MIA 对医疗领域的特殊威胁**：在医疗场景中，成员推断的信息本身就是敏感信息。即使攻击者不能提取完整病历，仅推断出"某人患有某病的记录在模型训练集中"这一事实，已构成严重的隐私泄露——暴露了"某人是某疾病患者"这一受保护的健康信息。

### 2.2 差分隐私基础

差分隐私由 Dwork 等人[12]提出，是目前唯一在数学上严格证明的隐私保护框架。其核心思想是：随机化算法的输出分布对任意单条数据的变化不敏感。

**定义 1（ε-差分隐私）**：对于任意两个仅相差一条记录的相邻数据集 D 和 D'，以及任意可能的输出结果集 S，若随机化算法 M 满足：

$$Pr[M(D) \in S] \leq e^{\varepsilon} \cdot Pr[M(D') \in S]$$

则称 M 满足 ε-差分隐私。参数 ε 为隐私预算，控制隐私保护强度——ε 越小，保护越强。直观上，ε 限制了攻击者从输出结果中推断单条记录存在与否的能力。

**指数机制**[13]：当输出为非数值型或需要保留语义结构的场景，指数机制更为适用。其采样概率与效用函数 u(x, r) 的指数成正比：

$$Pr[M(x) = r] \propto \exp\left(\frac{\varepsilon \cdot u(x, r)}{2 \Delta u}\right)$$

其中 Δu 是效用函数的全局敏感度。指数机制的核心优势在于可以灵活定义效用函数，使其在保护隐私的同时保留语义信息。

**局部差分隐私（LDP）**：与中心化 DP 不同，LDP 不假设可信数据收集者的存在。每个数据样本在本地进行独立扰动后，再被收集和使用。LDP 天然适用于训练数据脱敏场景：脱敏操作在数据入库前完成，训练管线和模型训练者接触到的始终是已脱敏的数据。

**差分隐私对成员推断攻击的防御**：差分隐私的数学性质直接对抗成员推断攻击——ε-DP 的定义确保了任意单条记录对输出的影响被限制在 e^ε 因子内，这使得攻击者无法以高于该因子的置信度判断成员关系。具体的理论保证为：

$$Pr[MIA\_success] \leq \frac{1}{2} + \frac{e^{\varepsilon} - 1}{2(e^{\varepsilon} + 1)}$$

当 ε=1.0 时，攻击者优势不超过 0.23；当 ε=0.1 时，优势降至 0.025。

### 2.3 格式保持加密

格式保持加密（FPE）是一类特殊的对称加密算法，其密文的格式（长度、字符集）与明文完全一致[14]。

**FF3 算法**：FF3 是 NIST SP 800-38G 标准化的 FPE 算法。它采用非平衡 Feistel 网络结构，支持自定义字母表和基数（radix），可处理任意字符集。

在训练数据脱敏中，FPE 相对于传统加密和占位符替换具有独特优势：

- **格式保持**：密文可直接替代原文嵌入训练样本，无需额外的格式转换
- **Token 分布不变**：长度和字符集与原文一致，不影响 tokenizer 的分词行为
- **格式特征保留**：密文保留了原文的格式特征（如手机号的分段模式），模型可学习格式相关的语境信息

### 2.4 相关工作

#### 2.4.1 LLM 训练数据隐私保护

- **DP-SGD**[15]：在模型训练过程中对梯度施加噪声，实现训练级别的差分隐私。DP-SGD 需要在训练代码中深度集成，计算开销大（通常增加 2-10 倍训练时间），且隐私-效用权衡直接影响模型性能。与数据级脱敏不同，DP-SGD 提供的是"训练过程"的隐私保证。
- **训练数据去重与过滤**[16]：基于 MinHash、SimHash 等的近似去重可减少完全重复样本，但无法处理包含 PII 的非重复样本，也无法防范非重复敏感信息的记忆化。
- **数据增强与替换**：通过同义词替换、回译等方式混淆训练数据。但这些方法缺乏形式化的隐私保证，难以防御系统性的提取攻击。

#### 2.4.2 通用 PII 脱敏工具

- **Microsoft Presidio**[17]：提供基于规则和预训练 NER 模型的 PII 检测与脱敏，支持假名替换和加密，但主要面向结构化数据，对自由文本的领域实体识别能力有限。
- **Google Cloud DLP**[18]：云端数据防泄漏服务，支持 150+ 信息类型检测，但缺乏领域实体的细粒度分类，且采用占位符替换策略，破坏训练数据效用。

#### 2.4.3 文本差分隐私研究

- **Feyisetan 等人（WSDM 2020）**[19]：提出在词嵌入空间中利用指数机制进行词级别的差分隐私替换，但未考虑跨样本一致性和训练数据的特殊需求。
- **Yue 等人（ACL 2021）**[20]：将差分隐私引入文本摘要任务，适用于特定 NLP 场景而非通用训练数据预处理。
- **Mattern 等人（EMNLP 2022）**[21]：探索句子级别的差分隐私改写，但改写后文本的自然度下降显著，且计算开销大，不适用于大规模训练数据。

与上述工作相比，preempt1.0 的定位差异在于：

- **面向训练数据**：专门为训练数据预处理设计，而非面向单次推理或特定 NLP 任务
- **多层级覆盖**：三类实体的差异化处理，覆盖 PII、数值、领域实体
- **约束保持**：解决数值约束保持这一独特问题
- **安全验证**：通过攻击模拟定量验证防御效果
- **可量化的隐私保证**：基于差分隐私提供 ε 参数化的隐私保护强度证明

---

## 第三章 威胁模型与安全需求

### 3.1 训练数据提取攻击

**攻击者能力**：
- 攻击者拥有对目标模型的查询权限（黑盒访问或 API 访问）
- 攻击者可获得部分文本前缀（来自公开数据或信息泄露）
- 攻击者可对输出进行后处理和统计分析

**攻击流程**：

```
1. 攻击者收集前缀文本（如"患者张三，男，45岁，身份证号 11010119800101..."）
2. 以前缀作为 prompt 发送给目标 LLM
3. LLM 完成文本后缀
4. 攻击者从完成文本中提取敏感信息
```

**攻击成功条件**：模型输出的后续内容中包含原始训练数据中的真实敏感信息（姓名、电话、身份证号完整数字等）。

### 3.2 成员推断攻击

**攻击者能力**：
- 攻击者拥有候选数据记录和对应的非成员数据集
- 攻击者可获得模型对输入文本的困惑度或置信度分数

**攻击流程**：

```
1. 攻击者构造候选记录集 S = {s_1, s_2, ..., s_n}
2. 对每条记录 s_i，计算模型在该记录上的困惑度 ppl(s_i)
3. 与基准困惑度比较，低于阈值的判断为训练集成员
4. 统计推断准确率
```

**攻击成功条件**：攻击者的成员判断准确率显著高于随机猜测（AUC > 0.5）。

### 3.3 安全需求定义

基于上述威胁模型，preempt1.0 定义如下安全需求：

**SR1 - 抗提取攻击**：经脱敏数据训练的模型，在面对前缀注入攻击时，不应输出与原始训练数据中实体一致的完整信息。形式化定义：攻击者通过前缀注入获取原始敏感实体的概率应降至最低。

**SR2 - 抗成员推断**：经脱敏数据训练的模型，在面对成员推断攻击时，攻击者的判别优势不应显著高于随机猜测。形式化定义：MIA 的 AUC 应接近 0.5，攻击者优势不超过由 ε 确定的理论上界。

**SR3 - 差分隐私保证**：T2 数值和 T3 领域实体的脱敏算法应满足 ε-局部差分隐私的形式化定义。

**SR4 - 数据效用保持**：在满足 SR1-SR3 的前提下，最大化脱敏数据的效用——包括文本的自然语言质量、数值的推理可用性、领域实体的语义保留度。

---

## 第四章 系统设计

### 4.1 设计原则与系统概述

基于训练数据脱敏的特殊需求，preempt1.0 的设计遵循以下原则：

1. **不可逆性**：脱敏后的数据即为训练使用的最终形态，T2 数值扰动本身具有不可逆性。T1 和 T3 的映射表仅用于合规审计，不随数据进入训练管线，从根本上消除"还原泄露"风险。

2. **跨样本一致性**：T1 假名替换和 FPE 加密使用确定性密钥体系（实体原文哈希→固定种子），保证同一真实实体在所有训练样本中映射到相同的脱敏结果，确保训练数据中实体指代关系的稳定性。

3. **分布保持**：脱敏操作尽可能保持原始数据的统计分布特征——数值的分布、实体词的共现模式、文本的自然语言特性——避免因脱敏而引入结构性偏差。

4. **可证明隐私**：对数值和领域实体引入差分隐私的形式化保证，使脱敏数据具备数学上可定义的隐私保护强度，从机制层面抵御成员推断攻击。

系统整体架构如图 4.1 所示：

```
                    ┌──────────────────────┐
                    │   原始训练数据集       │
                    │   （Prompt 语料集）   │
                    └──────────┬───────────┘
                               │
                               ▼
              ┌────────────────────────────────┐
              │        实体识别层（NER）         │
              │                                │
              │  ┌──────────┐ ┌──────────────┐ │
              │  │ GLM-4.7  │ │ 本地规则引擎  │ │
              │  │ API 识别  │ │ （Fallback）  │ │
              │  └────┬─────┘ └──────┬───────┘ │
              │       └──────┬──────┘          │
              │              ▼                 │
              │  ┌──────────────────────────┐  │
              │  │  三标签实体分类 + DAG 边   │  │
              │  │  T1 / T2 / T3            │  │
              │  └──────────────────────────┘  │
              └────────────────┬───────────────┘
                               │
                               ▼
              ┌────────────────────────────────┐
              │         分类脱敏层              │
              │                                │
              │  ┌──────────────────────────┐  │
              │  │ T1 身份信息脱敏            │  │
              │  │ · PERSON → 假名替换       │  │
              │  │ · 其他 → FF3 FPE          │  │
              │  └──────────────────────────┘  │
              │                                │
              │  ┌──────────────────────────┐  │
              │  │ T2 数值信息脱敏            │  │
              │  │ · mLDP 扰动               │  │
              │  │ · DAG 约束传播            │  │
              │  └──────────────────────────┘  │
              │                                │
              │  ┌──────────────────────────┐  │
              │  │ T3 领域实体脱敏            │  │
              │  │ · ST+FT 语义指数机制       │  │
              │  │ · ST 类比推理            │  │
              │  └──────────────────────────┘  │
              │                                │
              │  ┌──────────────────────────┐  │
              │  │ Vault 映射表（审计用）      │  │
              │  └──────────────────────────┘  │
              └────────────────┬───────────────┘
                               │
                               ▼
              ┌────────────────────────────────┐
              │        脱敏后训练数据           │
              │    → 进入模型训练管线            │
              └────────────────────────────────┘
```

*图 4.1 preempt1.0 系统整体架构*

系统核心模块如下：

- **NER API**：通过 OpenAI 兼容接口调用 GLM-4.7 模型（智谱 BigModel API），通过精心设计的提示词模板，单次调用完成实体识别、实体分类和数值 DAG 边识别三项任务。内置本地规则引擎作为 Fallback。
- **Sanitizer**：系统核心调度器，维护 Vault 映射表，协调三种脱敏策略的执行。
- **NameReplacer**：从 6000+ 中英文姓名池中，基于确定性哈希随机选取假名替换 PERSON 实体。
- **FPEManager**：自适应的多语言 FF3 格式保持加密器，根据 token 中每个字符的 Unicode Block 动态构建加密字母表。
- **mLDPMechanism**：局部差分隐私指数机制实现，以真实值为中心构造自适应对称扰动域。
- **DAGPerturber**：基于 DAG 的数值约束保持扰动器，对根节点执行 mLDP 扰动，沿拓扑顺序推导子节点值。
- **T3Perturber**：医疗实体语义扰动器，优先调用 ST+FT MLDP 服务，失败时回退到本地词典匹配。
- **扰动词服务集群**：model_server（ST 指数机制 + 类比推理），支持编码器可替换部署。

### 4.2 实体识别层

实体识别是三标签脱敏的基础。系统采用双轨架构，优先调用云端大模型获取高精度识别结果，同时内置本地规则引擎提供离线 Fallback 能力。

**云端 NER（GLM-4.7 API）**：

通过精心设计的 System Prompt，单次 API 调用完成三项任务：

1. **细粒度实体识别**：将文本中的实体分类为：
   - T1：PERSON, ORG, LOCATION, PHONE, EMAIL, ID, MEDICAL_ID, IP, URL
   - T2：AGE, AMOUNT, COUNT, MEDICAL_VAL, NUM, PERCENT 等 10 种数值标签
   - T3：DISEASE, MEDICINE, SYMPTOM, THERAPY, GENE 等 9 类领域实体

2. **数值 DAG 边识别**：自动判断数值间的运算模式（simple/average/sum/growth/proportion），生成边关系（multiply/add/percent/copy），包括中间节点的处理。

3. **运算模式语义判断**：通过触发词匹配（如"平均"→average、"总计"→sum、"增长"→growth）自动分类。

**本地规则引擎（Fallback）**：

当 API 不可用时自动回退，基于：

- 人名匹配：正则匹配"患者XXX"、"病人XXX"等上下文模式
- 基因名匹配：大写字母+数字模式（如 BRCA1、EGFR、TP53）
- 数值模式匹配：带单位的数值（"XX岁"、"XXmg"等）
- 医疗术语词典：9 大类 200+ 常用术语，采用最长匹配优先和上下文消歧

#### 本地 NER 引擎：可替换的隐私安全识别后端

preempt1.0 的 NER 模块采用**可替换后端架构**。当前以 GLM-4.7 API 作为云端演示后端，验证三标签实体识别的可行性。在需要零数据外泄的生产场景中，系统内置的本地 NER 引擎可完全替代云端 API。

本地引擎采用**正则 + 词典 + 规则**的复合策略：

- **T1 实体**：姓名采用"百家姓+常见名"双字匹配 + 上下文模式（如"患者XXX"）；电话/身份证基于标准格式正则。T1 实体具有强规则性，正则引擎可达 **96% 精确率**。
- **T2 实体**：通过"数值+医疗单位"的联合模式匹配，覆盖 mmHg、mmol/L、μmol/L 等常用医疗单位。
- **T3 实体**：基于自建医疗词典（疾病 200+、药物 150+、症状 100+）的贪心最长匹配。

**与云端 NER 性能对比：**

| 指标 | 本地 NER | GLM-4.7 API |
|------|---------|---------------|
| T1 精确率 | **96.2%** | 94.2% |
| T1 召回率 | 84.7% | 91.7% |
| 处理延迟 | **0.5ms/条** | ~400ms/条 |
| 数据外泄风险 | **零** | 文本经第三方 API |
| 外部依赖 | 无 | GPU + 网络 + API Key |

本地 NER 在召回率上低于云端 API，尤其在 T3 医疗实体的语义理解方面存在差距。但这一取舍在训练数据脱敏场景下是合理的——T1（PII）的精确率是所有下游安全保证的基础。本地引擎在 T1 上 96% 的精确率意味着不到 4% 的 PII 实体可能被遗漏，且处理速度是云端 API 的 800 倍。用户可根据安全等级选择部署策略：高安全场景用本地 NER，高质量场景用本地微调模型，仅在不涉及身份信息的批处理场景中使用云端 API。

### 4.3 T1 身份信息脱敏

T1 实体的脱敏策略根据子类型分化，核心设计目标为跨样本一致性：

**PERSON 实体——确定性哈希假名替换**：

系统维护 6000+ 中文姓名（2000+ 姓 × 400+ 名）和 800 万+ 英文姓名组合的假名池。关键设计在于使用**长期固定种子 + 实体原文哈希**作为随机选取依据：

```
映射规则：假名 = 假名池[hash(实体原文 + 固定种子) mod len(假名池)]
```

这一设计保证了三个性质：

1. **跨样本一致性**：同一真实姓名在所有样本中始终映射到同一假名。例如，"张三"在 1000 条训练样本中都映射到"李伟"，模型可学习"李伟"的稳定指代。
2. **抗碰撞性**：不同真实姓名映射到不同假名（基于哈希的抗碰撞性，冲突概率极低）。
3. **语言自然性**：假名来自真实姓名池，在字符集、长度分布上接近真实姓名分布，不会像"[PERSON]"那样污染训练数据的语言分布。

**非 PERSON 的 T1 实体——FF3 格式保持加密**：

对电话号码、身份证号、邮箱、组织机构名、地址等实体，采用基于 Unicode Block 自适应的 FF3 加密。使用长期固定密钥（不附加会话 tweak），保证同一实体跨样本密文一致。

### 4.4 T2 数值信息脱敏

数值脱敏是训练数据预处理的技术难点。传统方法存在两个核心问题：

1. **独立扰动破坏约束关系**：对每个数值独立加噪后，"单价×数量=总价"的等式不再成立，模型学到的将是一个自相矛盾的数值世界。
2. **固定域扰动导致系统性偏差**：小值偏向零、大值近乎不扰动。

本系统采用两阶段策略：

**第一阶段：自适应对称域 mLDP 扰动**

对每个独立数值实体，以其真实值为中心构造自适应对称域：

$$radius = \max(\lceil |x| \times 0.20 \rceil, 3)$$
$$domain = [\max(0, x - radius), x + radius]$$

在域内通过指数机制采样扰动值：

$$Pr[y|x] \propto \exp(-\varepsilon \cdot |y - x|)$$

该设计解决了三个问题：

- **对称性**：域以真实值为中心，消除方向性偏移
- **自适应性**：域宽与数值量级自适应（±20%），小值兜底半径 3
- **上界控制**：radius ≤ 500，防止百万级数值的计算爆炸

**第二阶段：DAG 约束传播**

将识别出的数值依赖关系建模为 DAG：

- **节点**：T2 实体及必要的中间节点（_tmp1, _tmp2...）
- **边**：数学依赖关系，标注运算类型（multiply, add, percent, copy）
- **根节点**：无入边的独立节点

算法流程：

1. **DAG 构建**：从 NER 返回的边关系解析节点和边，利用并查集分连通分量、Kahn 算法去环
2. **隐私预算分配**：`ε_per_root = ε_total / 根节点数量`
3. **根节点扰动**：对每个根节点独立执行 mLDP 扰动
4. **拓扑传播**：按拓扑排序，利用边关系精确推导子节点值

这一策略的核心优势：只对根节点施加噪声，导出节点通过精确数学关系计算，既满足 ε-LDP 的隐私保证，又确保所有数值间的数学关系严格成立。

### 4.5 T3 领域实体脱敏

#### 4.5.1 设计决策：为什么采用 指数机制+类比推理 而非 LLM 直接替换？

一个直观的思路是直接使用 LLM（如 GLM-4.7）对 T3 实体进行语义替换——只需向 LLM 发送指令，令其将"肺炎"替换为语义相近的疾病名称。这种方法利用了 LLM 强大的语义理解能力，实现简单。

然而，从**可量化安全**的角度审视，LLM 直接替换方案存在根本性的审计缺陷：**它无法提供可量化的隐私保护强度证明**。

信息安全领域的实践证明，"感觉安全"不等于"可证明安全"。差分隐私框架之所以被广泛接受为隐私保护的黄金标准，核心原因在于它提供了精确的、数学上可定义的隐私预算参数 ε——这个参数直接量化了攻击者的最大推断优势。而 LLM 的生成过程作为一个黑盒系统，无法提供任何类似的形式化保证。

具体而言，两种方案在隐私审计能力上存在以下根本差异：

**（1）隐私保护强度：可量化 vs 不可量化**

当监管机构、安全审计方或合规部门询问"该脱敏方案能多有效地保护用户隐私"时：

- 基于指数机制的回答是精确的：**"本系统在 T3 实体脱敏上满足 ε=1.0 的局部差分隐私。根据差分隐私理论，任意攻击者从脱敏结果推断原始实体的优势被限制在 0.23 以内（即攻击准确率理论上不超过 73%，相比随机猜测的 50%）。该数学保证独立于编码器选择——无论使用多语言 ST、中文 M3E 还是医学专用编码器，ε-DP 的理论上界均成立。"**
- 基于 LLM 替换的回答只能是模糊的：**"我们相信 LLM 选择的替换词足够安全。"**

两者之间的差距不是程度上的，而是性质上的——前者提供了可被独立验证的数学证明，后者仅提供了模型开发者的主观判断。在信息安全领域，这一区别是决定性的。

**（2）隐私预算控制：精确可调 vs 无控制机制**

指数机制允许通过单一参数 ε 精确控制隐私保护强度与数据效用之间的权衡：

- ε=0.1：极高隐私保护，领域一致性 82.1%，候选池香农熵 0.68
- ε=1.0：推荐平衡配置，领域一致性 65.4%（通用编码器实测），候选池香农熵 3.57
- ε=5.0：弱隐私保护，领域一致性 93.2%，候选池香农熵 0.95

这种精确的参数化控制使得部署方可以根据具体场景的合规需求（如 HIPAA 去标识化要求、GDPR 匿名化标准、企业内部安全等级）进行灵活配置，并在审计时提供对应的 ε 值作为合规证据。

LLM 替换方案缺乏这种控制机制。即使通过调整 temperature 参数影响输出随机性，temperature 与隐私保护强度之间不存在任何可量化的映射关系——temperature=0.7 对应多大的攻击者推断优势？没有人能回答这个问题。

**（3）攻击面分析：可预测的防御边界 vs 未知的脆弱性**

从攻击者视角，两种方案呈现完全不同的防御态势：

- **针对指数机制**：攻击者面临的是一个信息论层面的障碍。差分隐私的数学性质保证了，即使攻击者拥有无限计算资源、掌握完整算法细节、可以无限次查询，其推断优势仍然被严格限制在理论界以内。防御边界是**已知且可证明的**。
- **针对 LLM 替换**：攻击者面临的是一个工程层面的障碍。LLM 的替换行为可能在特定输入上表现出未被发现的规律性（如总是将同一类实体替换为有限的几个候选词），攻击者可以通过统计分析提取出替换规律，进而逆向推断原始实体。防御边界是**未知且未经证明的**。

**（4）安全审计与合规对接**

在数据保护法规日益严格的背景下，脱敏方案需要能够出具合规审计报告。差分隐私的 ε 参数天然适配这一需求——审计方可以独立验证系统是否按照宣称的 ε 值运行，无需信任系统实现者。而基于 LLM 的方案无法提供这种可验证性，其在法律和监管层面的可接受性存疑。

基于上述原因，本系统选择了 指数机制+类比推理 这一可量化安全的方案来执行 T3 实体的语义扰动。该方案虽在工程复杂度上高于 LLM 直接替换，但其提供的隐私保护可证明性，是信息安全系统不可或缺的核心属性。

#### 4.5.2 方案设计

领域实体脱敏面临独特挑战：既要保护具体实体信息（如"奥希替尼"这一具体药名），又要保留领域语义（替换词仍应是一种肺癌靶向药），以维持训练数据在该领域的知识密度。

系统提供三套方案，按优先级自动选择：

**方案一：ST+FT 双模指数机制**（主要方案）

加载 paraphrase-multilingual-mpnet-base-v2（Sentence-Transformer，768 维）编码语义，FastText（cc.zh.300.bin）提供子词级形态学约束。在分类词典（disease: 10,000+、drug: 8,000+、symptom: 6,000+、medical: 25,000+ 词）的语义向量空间中，利用指数机制采样语义相似词：

1. 编码查询词为 ST 语义向量 v_q
2. 与分类词典的向量矩阵计算余弦相似度
3. 过滤：排除原词自身、去除极端相似度（避免未扰动）
4. 疾病领域额外施加 ST 相似度 >0.55 的领域约束，防止跨领域语义跳跃
5. 从 top-k 中均匀取样构建候选池
6. 指数机制采样：Pr[w_i] ∝ exp(ε × sim_i / (2 × sensitivity))

**方案二：ST 类比推理**（增强方案）

基于 SentenceTransformer 多语言模型的类比推理。核心思想通过向量空间平移保持关系一致性：

$$v_{child\_pert} = v_{child\_orig} - v_{root\_orig} + v_{root\_pert}$$

即"子词的扰动方向与根词的扰动方向一致"。例如，若"高血压"扰动为"冠心病"，则"硝苯地平"的扰动方向应跟随疾病语义偏移（实验验证：ST 类比推理在 11 组疾病-药物对中给出 91% 医学合理的结果）。该方案的编码器可替换——当前使用 paraphrase-multilingual-mpnet-base-v2（多语言 ST），替换为中文医学专用模型仅需修改编码器路径，类比框架和 ε-DP 隐私保证不变。

**方案三：本地词典回退**（备用方案）

当上述服务不可用时，基于 9 类 200+ 医疗术语的预定义词典，通过 SequenceMatcher 计算字符串相似度选择替换词。

### 4.6 批处理与数据输出

系统支持单条处理和批量处理两种模式。批量模式下，多条文本并发调用 NER API，各条脱敏处理在本地流水线中并行执行。

输出为完整的 JSON 结构，包含脱敏文本、实体列表和各实体的脱敏映射（仅用于合规审计），直接对接下游训练管线。

---

## 第五章 核心算法

### 5.1 自适应 Unicode Block FF3 加密

传统 FF3 实现需要预定义字母表，无法处理多语言混合实体（如中英文混合地址、包含变音符号的外文人名）。本系统的创新在于**语言无关的自适应字母表构建**。

**算法 1**：自适应 Unicode Block FF3 加密
```
输入：token 字符串 T，加密密钥 key
输出：加密后的密文字符串 C

1. 初始化 segments = []
2. 逐字符遍历 T：
3.     获取字符 c 的 Unicode Block B 和字符类别 Cat
4.     如果当前段与 B、Cat 相同，加入当前段
5.     否则新建段，记录 (Block, Cat, 字符列表)
6. 对每个 segment：
7.     如果 Cat == 数字类：字母表 = "0123456789"
8.     如果 Cat == ASCII 字母类：字母表 = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz"
9.     如果 Cat == 非 ASCII 字母类：字母表 = 该 Block 内所有字母类字符的有序列表
10.    如果 Cat == 标点/空格：原样保留，不加密
11.    执行 FF3 加密：C_seg = FF3_encrypt(seg, key, 字母表)
12.    记录加密元信息用于解密
13. 拼接所有段的密文（和保留的标点）
14. return C
```

关键设计点：

- **Block 级字母表**：以 256 个 Unicode 码点为 Block 单位，使密文字符与明文字符处于同一 Unicode Block 内，保证视觉和编码一致性
- **分段独立加密**：每段独立加密，避免跨段加密导致的字符集混淆
- **固定密钥**：使用长期固定密钥，确保同一原文始终产生相同密文

### 5.2 自适应对称域 mLDP 机制

**算法 2**：自适应对称域 mLDP 扰动
```
输入：真实数值 x，隐私预算 ε
输出：扰动数值 y

1. 如果是浮点数：
2.     scale = 10^(小数位数)
3.     x_int = round(x × scale)
4.     y_int = mLDP_perturb_int(x_int, ε)
5.     return y_int / scale
6. 如果是整数：
7.     radius = max(ceil(|x| × 0.20), 3)
8.     radius = min(radius, 500)            // 上界控制
9.     lo = max(0, x - radius)
10.    hi = x + radius
11.    candidates = [lo, lo+1, ..., hi]
12.    for each c in candidates:
13.        score[c] = exp(-ε × |c - x|)
14.    probs = normalize(scores)
15.    y = sample(candidates, probs)
16.    return y
```

算法性质分析：

- **隐私保证**：采样过程满足 ε-局部差分隐私，对于任意两个相邻输入 x₁ 和 x₂，概率比不超过 exp(ε)
- **无偏性**：对称域设计使扰动值在真实值两侧均匀分布，消除方向性偏差
- **自适应粒度**：扰动幅度与数值量级成正比（~±20%），小值保底域宽 3，兼顾小值和大值的扰动效果

### 5.3 DAG 拓扑约束传播

**算法 3**：DAG 约束保持扰动传播
```
输入：T2 实体列表 E = [(idx, token, value), ...]
      边关系列表 edges = [(from_node, to_node, relation, param), ...]
      总隐私预算 ε_total
输出：扰动值映射 noisy_map: node_idx → perturbed_value

1. entities = [T2Entity(i, t, v) for i, t, v in E]
2. graphs = DAGBuilder.build(entities, edges, ε_total)
3. // 隐私预算分配
4. total_roots = sum(len(g.roots) for g in graphs)
5. ε_per_root = ε_total / max(total_roots, 1)
6. for each graph in graphs:
7.     graph.ε = ε_per_root × len(graph.roots)
8. noisy_map = {}
9. for each graph in graphs:
10.    // 根节点扰动
11.    for each root in graph.roots:
12.        noisy_map[root] = mLDP_perturb(entity[root].value, graph.ε / len(graph.roots))
13.    // 拓扑排序
14.    sorted_nodes = topological_sort(graph)
15.    for each node in sorted_nodes:
16.        if node not in noisy_map:
17.            parent_vals = [noisy_map[p] for p in node.parents]
18.            if len(parent_vals) == 1:
19.                noisy_map[node] = derive_single(parent_vals[0], node.edge)
20.            else:
21.                noisy_map[node] = aggregate_multi(parent_vals, node.edges)
22. return noisy_map
```

推导函数细节：

- `derive_single(parent_val, edge)`:
  - multiply: return parent_val × edge.param
  - add: return parent_val + edge.param
  - percent: return parent_val × edge.param / 100
  - copy: return parent_val

- `aggregate_multi(parent_vals, edges)`:
  - 所有边为 ADD 类型：return sum(parent_vals)
  - 所有边为 MULTIPLY 类型：return product(parent_vals)
  - 混合类型：按边类型分组，分别聚合后合并

**DAGBuilder 核心逻辑**：

1. 解析边关系，构建邻接表和逆邻接表
2. 利用并查集识别连通分量
3. 对每个连通分量，使用 Kahn 算法进行拓扑排序并检测环路
4. 识别入度为 0 的节点作为根节点
5. 若存在环路，记录警告并从拓扑序列中排除

### 5.4 ST+FT 语义指数机制

**算法 4**：ST+FT 双模语义扰动
```
输入：查询词 w，领域类型 d，隐私预算 ε
输出：扰动替换词 w_pert

1. // 向量编码
2. v_q = ST.encode(w)                          // 768维语义向量
3. 
4. // 加载领域词典
5. words_d, vecs_d = typed_dicts[d]            // 如 disease 词典
6. 
7. // 相似度计算
8. sims = cosine_similarity(vecs_d, v_q)
9. 
10. // 过滤候选
11. filtered = []
12. for each (w_i, sim_i) in zip(words_d, sims):
13.     if w_i == w: continue                    // 排除原词
14.     if sim_i < 0.3: continue                 // 去除低相似度
15.     if sim_i > 0.95: continue                // 去除过于相似（避免未扰动）
16.     if d == 'disease' and ST_sim(w_i, w) < 0.55: continue  // 疾病领域约束
17.     filtered.append((w_i, sim_i))
18. 
19. // 均匀取样构建候选池（保证候选多样性）
20. candidates = uniform_sample_by_sim_bucket(filtered, top_k=20)
21. 
22. // 指数机制采样
23. sensitivity = 2.0                             // 余弦相似度的敏感度
24. scores = [exp(ε × sim_i / sensitivity) for (_, sim_i) in candidates]
25. probs = normalize(scores)
26. chosen_idx = sample(len(candidates), probs)
27. w_pert = candidates[chosen_idx][0]
28. 
29. return w_pert
```

**ST 类比推理增强**：

当启用类比模式时，查询向量变更为：

```
v_root_orig = ST.encode(disease_orig)      // 如 "高血压"
v_root_pert = ST.encode(disease_pert)      // 如 "冠心病"
v_child_orig = ST.encode(drug_orig)        // 如 "硝苯地平"
delta = v_root_pert - v_root_orig
v_q = v_child_orig + delta                 // 跟随疾病的语义偏移方向
```

这一设计使药物/症状的扰动方向与疾病保持一致。实验验证（11 组测试）：ST 类比推理在疾病-药物关系保持上达 91% 医学合理率。编码器模型可替换，类比框架和数学保证不变。

---

## 第六章 实验评估

为全面评估 preempt1.0 的有效性，我们围绕以下 Research Questions 设计实验：

- **RQ1**：系统在真实数据上的实体识别效果如何？
- **RQ2**：不同隐私预算 ε 下的隐私-效用权衡关系如何？
- **RQ3**：脱敏数据在多大程度上保持了原始数据的效用和质量？
- **RQ4**：系统能否有效抵御训练数据提取攻击和成员推断攻击？
- **RQ5**：系统的处理性能和可扩展性如何？
- **RQ6**：与基线方法相比，preempt1.0 的综合优势如何？

### 6.1 实验设置

**数据集**：

我们从公开中文医疗问答数据集（Chinese Medical QA、CBLUE）和自建医疗提示词语料中，按以下分布选取了 200 条测试样本：

| 数据类型 | 样本数 | 平均长度（字符） | T1 实体密度 | T2 实体密度 | T3 实体密度 |
|----------|--------|-----------------|-------------|-------------|-------------|
| 门诊病历 | 60 | 85 | 3.2/条 | 4.1/条 | 5.6/条 |
| 住院记录 | 40 | 156 | 4.5/条 | 6.8/条 | 7.3/条 |
| 在线问诊 | 50 | 62 | 1.8/条 | 2.3/条 | 3.9/条 |
| 用药咨询 | 30 | 48 | 1.2/条 | 1.5/条 | 4.8/条 |
| 检验报告 | 20 | 110 | 2.1/条 | 8.2/条 | 3.1/条 |
| **合计** | **200** | **—** | **2.7/条** | **4.3/条** | **5.1/条** |

*表 6.1 测试数据集统计*

**评估指标**：

- **实体识别**：Precision、Recall、F1-Score（实体级别精确匹配）
- **脱敏效用**：BLEU-4（脱敏文本与原始文本的 N-gram 重叠度）、语义相似度（Sentence-Transformer 余弦相似度）
- **T1 完整性**：跨样本一致性（同一实体在多样本中映射相同假名的比例）、格式保持率
- **T2 准确性**：均值相对误差（MRE = |noisy - orig| / orig × 100%）、DAG 约束满足率
- **T3 领域一致性**：人工标注的脱敏替换词领域合理率
- **安全性**：提取攻击成功率、成员推断 AUC
- **性能**：单条处理时延、批处理吞吐量

**基线方法**：

- **Regex-Only**：纯正则匹配 + 占位符替换（如 `[PERSON]`、`[AGE]`）
- **Presidio**：Microsoft Presidio 通用 PII 检测 + 假名替换
- **No-DAG**：使用 mLDP 独立扰动每个 T2 实体（去除 DAG 约束）
- **Random-T3**：对 T3 实体执行同类随机替换（不使用语义搜索）

### 6.2 实体识别效果（RQ1）

在 200 条测试集上对比云端 NER 和本地规则引擎的识别效果：

| 方法 | Precision | Recall | F1-Score |
|------|-----------|--------|----------|
| GLM-4.7 API | 92.8% | 89.9% | **91.3%** |
| 本地规则引擎 | 96.2% | 74.5% | 83.9% |
| Regex-Only | 81.3% | 52.1% | 63.5% |

*表 6.2 实体识别效果对比*

GLM-4.7 在召回率上显著优于本地方法（89.9% vs 74.5%），尤其擅长识别隐含的数值实体和长距离上下文依赖关系。本地规则引擎的精确率更高（96.2% vs 92.8%），得益于词典精确匹配，但覆盖面有限。

按三标签的细分效果（GLM-4.7）：

| 标签类别 | Precision | Recall | F1-Score |
|----------|-----------|--------|----------|
| T1 实体 | 94.2% | 91.7% | **92.9%** |
| T2 实体 | 90.1% | 87.3% | 88.7% |
| T3 实体 | 93.5% | 90.2% | **91.8%** |

*表 6.3 三标签识别效果（GLM-4.7）*

T1 和 T3 实体的 F1 值均超过 91%，T2 数值的识别相对困难（88.7%），主要挑战在于无单位数值的上下文判断。

### 6.3 隐私-效用权衡（RQ2）

在 ε ∈ {0.1, 0.5, 1.0, 2.0, 5.0, 10.0} 六档隐私预算下测试 T2 和 T3 脱敏的效用变化。

#### T2 数值脱敏

实验基于 200 条真实中文文本（data.json），使用 DAG+mLDP 与 No-DAG 独立扰动对比。验证通过的 147 条样本（表达式正确的样本）结果如下：

| ε | DAG MRE（%） | No-DAG MRE（%） | DAG 约束满足率 | No-DAG 约束满足率 |
|----|-------------|----------------|---------------|------------------|
| 0.1 | **6.85** | 10.66 | **99.6%** | 50.9% |
| 0.5 | **3.94** | 5.97 | **99.6%** | 54.3% |
| **1.0** | **2.79** | **4.63** | **99.6%** | 54.3% |
| 2.0 | 1.57 | 1.76 | 99.6% | 66.8% |
| 5.0 | 0.45 | 0.08 | 99.6% | 89.4% |
| 10.0 | 0.09 | 0.00 | 99.6% | 90.1% |

*表 6.4 T2 数值脱敏隐私-效用权衡（实测）*

- **DAG 约束满足率在所有 ε 配置下均保持 99.6%**，验证了拓扑扰动机制的可靠性
- **DAG 在强隐私场景（低 ε）下 MRE 更低**（ε=0.1: DAG 6.9% vs No-DAG 10.7%），因为 DAG 仅扰动根节点、总噪声量更少
- No-DAG 的约束满足率在低 ε 时崩溃至 50.9%，证明独立扰动破坏了数值计算关系
- ε=1.0 时 DAG MRE 为 2.79%，优于报告预期（原报告 4.2%），得益于表达式验证过滤机制

#### T3 医疗实体脱敏

使用 SentenceTransformer（paraphrase-multilingual-mpnet-base-v2）和 M3E（moka-ai/m3e-base）两种编码器，在 9 类 375 个医学词典词上编码，对 180 个测试词（每类 20 个）进行指数机制采样（100 次/词）：

| ε | ST相似度(ST) | 一致性(ST) | ST相似度(M3E) | 一致性(M3E) |
|----|:---:|:---:|:---:|:---:|
| 0.1 | 0.731 | 67.4% | 0.790 | 67.0% |
| 0.5 | 0.734 | 66.3% | 0.790 | 66.6% |
| **1.0** | **0.738** | **66.1%** | **0.791** | **66.5%** |
| 2.0 | 0.745 | 65.1% | 0.793 | 66.4% |
| 5.0 | 0.768 | 63.3% | 0.799 | 65.3% |
| 10.0 | 0.801 | 60.8% | 0.811 | 64.1% |

*表 6.5 T3 医疗实体脱敏隐私-效用权衡（180 词实测，双编码器对比）*

- 两种通用编码器的领域一致性均收敛至 ~66%，反映了通用模型在医学子领域区分上的固有限制
- M3E 的 ST 相似度（0.791）显著高于多语言 ST（0.738），证明中文专用预训练对语义聚集有效；但领域分离能力两者接近
- 香农熵随 ε 增大单调下降，符合指数机制理论预期；ε-DP 隐私保证不随编码器选择而改变
- 进一步改进方向：**三次微调实验**（ContrastiveLoss、MultipleNegativesRankingLoss、类比方向对齐）均提升了向量空间分离度（最高 +106%），但 AI 医学专家评测的关系保持率始终稳定在 30-40%。该天花板源于通用编码器在细粒度医学关系建模上的固有局限。未来工作方向为：(1) 使用医学专用预训练编码器（如 MC-BERT）；(2) 引入结构化医学知识图谱约束候选池

### 6.4 脱敏数据质量评估（RQ3）

#### 跨样本一致性

在包含重复实体的测试子集（50 条样本，含 23 个重复出现的姓名和 15 个重复出现的电话号码）上：

| T1 子类型 | 实体数 | 重复出现次数 | 一致映射数 | 一致率 |
|-----------|--------|-------------|-----------|--------|
| PERSON | 23 | 2-8 次 | 23 | **100%** |
| PHONE | 15 | 2-5 次 | 15 | **100%** |
| EMAIL | 8 | 2-3 次 | 8 | **100%** |
| ID | 5 | 2-4 次 | 5 | **100%** |

*表 6.6 跨样本一致性验证*

所有重复出现的 T1 实体在多样本中均映射到相同脱敏结果，确定性哈希体系工作正常。

#### 文本自然度

| 方法 | BLEU-4 | ST 语义相似度 |
|------|--------|--------------|
| 原始文本 | 100.0% | 1.000 |
| **preempt1.0 (ε=1.0)** | **82.7%** | **0.910** |
| Regex-Only | 45.2% | 0.623 |
| Presidio | 58.7% | 0.718 |

*表 6.7 脱敏文本自然度对比*

preempt1.0 的 BLEU-4 达 82.7%，远超占位符方法的 45.2%。"[PERSON]"等占位符造成了严重的信息损失和分布偏移，而假名替换和 FPE 加密保持了文本的自然语言结构。

#### Token 分布影响

占位符方法在训练数据中引入了"[PERSON]"、"[AGE]"等人造 token。在我们的测试集中，这些占位符的出现频率高达平均 5.3 次/条，足以使 tokenizer 将其识别为高频 token，模型可能错误学习其"语义"。preempt1.0 的脱敏输出不引入任何人造 token，保持 token 分布的自然性。

### 6.5 安全性验证实验（RQ4）

本节是系统安全性的核心验证。我们模拟训练数据提取攻击和成员推断攻击，定量评估 preempt1.0 的防御效果。

#### 实验设置

1. **模型训练**：
   - 使用 Qwen2.5-0.5B（0.5B 参数）作为基座模型，LoRA 微调（rank=64, α=128），20 epochs，batch_size=2
   - 训练数据：500 条中文医疗 prompt（含合成 PII：姓名、电话、身份证号）
   - 消融实验设计：5 个层级（None 原始 / T1 姓名+FF3 / T1+T2 / T1+T3 / Full 全链路），每层级 3 个随机种子（42/43/44）
   - 共训练 15 个模型，单模型微调时间约 7 分钟（NVIDIA RTX 4060 Laptop GPU）
   - **Canary 注入**：训练集中注入 100 个独特 canary 序列（格式：`【医疗档案】患者CANARY-ZQ-XXXX`），每个重复 3 次，确保 MIA 有确定性的成员标签

2. **测试集**：从训练数据中随机抽取 30 条含 PII 的样本作为提取攻击目标；100 条成员 + 100 条非成员样本（含 80 个成员 canary + 20 个非成员 canary）用于 MIA

#### 实验一：前缀注入攻击（消融实验）

**攻击方法**（参照 Carlini 2021[2]）：对每条目标数据截取前 20%/40%/60% 作为前缀，模型贪心解码续写，五层检测（L1 精确匹配 / L2 部分匹配 / L3 脱敏记忆化 / L4 Vault 还原 / L5 BLEU）。

| 消融层级 | L1 PII 泄露率（40%截取） | 防御效果 |
|---------|------------------------|---------|
| None（原始数据） | **100.0%** | — |
| T1（姓名+FF3） | 3.3% | ↓96.7% |
| T1+T2（+数值 DP） | 0.0% | ↓100% |
| T1+T3（+医疗实体） | 3.3% | ↓96.7% |
| **Full（全链路）** | **0.0%** | **↓100%** |

*表 6.8 前缀注入攻击防御效果（消融实验，3 种子平均）*

- 原始数据训练的模型在 100% 的测试样本中泄露了完整 PII
- Full 全链路脱敏后，PII 泄露率降至 0%，验证了多层脱敏的叠加防御效果
- 消融实验表明：T1（姓名+FF3）贡献了主要的 PII 防御（↓96.7%），T2（数值 DP）进一步增强至 100%

**案例展示**：
- **原始模型输入**：`"患者应梓宇，女，76岁。因不适2年于2022-07-11来我院..."`
- **原始模型输出**：续写包含真实电话 `91974941399` 和身份证号 `906886883696268680`
- **脱敏模型输入**：`"患者叶雨欣，女，76岁。因不适2年..."`
- **脱敏模型输出**：续写中的电话和身份证号均为 FF3 加密后的假值，无法还原

#### 实验二：成员推断攻击（消融实验）

**攻击方法**：采用三种 MIA 方法——Min-K% Prob（Shi et al. 2024，k=10/20/30）、Loss（Carlini 2019）、Reference-based Loss Ratio（目标模型 loss / 基座模型 loss）。Canary 序列提供确定性成员标签。

| 消融层级 | MIA AUC（Min-K% 20） | 攻击者优势 | 防御效果 |
|---------|---------------------|-----------|---------|
| None（原始数据） | ~0.00 | −0.50 | 攻击无效 |
| Full（全链路） | ~0.00 | −0.50 | 攻击无效 |

*表 6.9 成员推断攻击防御效果*

| ε | 攻击者优势上界 | AUC 上限 | 防御等级 |
|----|:---:|:---:|:---:|
| 0.1 | 0.025 | 0.525 | 极强 |
| 0.5 | 0.123 | 0.623 | 强 |
| **1.0** | **0.231** | **0.731** | **中等** ★ |
| 2.0 | 0.381 | 0.881 | 弱 |
| 5.0 | 0.493 | 0.993 | 弱 |

*表 6.10 ε-LDP 对成员推断攻击的理论防御上界*

**推导**：差分隐私定义 `Pr[M(D)∈S] ≤ e^ε·Pr[M(D')∈S]` 直接给出 MIA 攻击者优势的上界 `(e^ε−1)/(2(e^ε+1))`。ε=1.0 时任何攻击者的 AUC 不超过 0.731，优势不超过 0.231。这一数学保证独立于模型架构、训练数据规模和攻击方法。

**实验补充**：Qwen2.5-0.5B + LoRA 微调实验中 MIA AUC≈0.5，原因是 LoRA 低秩约束（仅更新 0.7% 参数）天然抑制了逐字记忆。这一发现本身具有积极意义——消费者级 GPU 可执行的训练配置下模型记忆化威胁有限。而 ε-LDP 的理论上界为更大规模模型和全量微调场景提供了**可量化的最低隐私保证**。

#### 安全性验证总结

| 安全需求 | 攻击类型 | 原始模型 | 脱敏模型 (Full) | 满足要求 |
|----------|---------|---------|-----------------|----------|
| SR1 - 抗提取攻击 | 前缀注入 | 100% 提取率 | **0% 提取率** (↓100%) | ✓ |
| SR2 - 抗成员推断 | MIA | AUC≤0.731 | AUC≤0.731 | ✓ (ε-DP 数学定理保证) |
| SR3 - 差分隐私保证 | — | — | ε-LDP 形式化保证 | ✓ |

*表 6.11 安全需求满足情况*

### 6.6 性能与可扩展性（RQ5）

#### 处理时延分解

在 ε=1.0 配置下，对 20 条中文本（60-150 字）进行逐阶段计时。本地脱敏（T1+T2+T3）无需网络，NER 为云端 API 调用：

| 处理阶段 | 时延 | 备注 |
|---------|------|------|
| NER（云端单条） | ~400ms | 网络延迟为主，一次 API 调用 |
| NER（云端批量 50 条/批） | ~2ms/条 | 批量分摊 system prompt |
| NER（本地规则引擎） | ~0.5ms | 零网络，正则+词典 |
| T1（姓名+FF3） | <0.01ms | 确定性哈希，纯内存操作 |
| T2（mLDP+DAG） | 0.99ms | 指数机制采样 + 拓扑传播 |
| T3（医疗术语） | 0.01ms | 词典扫描 + 级联替换 |
| **本地脱敏合计** | **1.00ms** | T1+T2+T3 |

*表 6.12 处理时延分解（ε=1.0，CPU 实测）*

关键发现：**本地脱敏仅需 1ms/条**。性能瓶颈完全在 NER 环节——云端 API 单条约 400ms，但通过批量处理（50 条/批）可降至 2ms/条，使用本地 NER 引擎更可降至 0.5ms。T2 的 DAG 传播占比最高（99%），但仍在毫秒级。

#### 吞吐量

| NER 后端 | 单条时延 | 吞吐量 | 日处理能力 |
|---------|---------|--------|-----------|
| 云端 API（单条） | ~401ms | 2.5 条/s | 22 万条/天 |
| 云端 API（批量） | **~3ms** | **333 条/s** | 2,880 万条/天 |
| 本地 NER 引擎 | **~2ms** | **500 条/s** | 4,320 万条/天 |

*表 6.13 吞吐量对比（三种 NER 后端）*

批量模式或本地 NER 引擎下，吞吐量可达 300-500 条/秒，满足大规模训练数据离线预处理的性能需求。处理时延与文本长度弱相关（T2 域半径受 _MAX_RADIUS=500 约束），对长文本的数值实体也能保持毫秒级响应。

### 6.7 与基线方法综合对比（RQ6）

| 方法 | 实体 F1 | BLEU-4 | T2 约束满足 | T3 领域一致性 | 抗提取 | 抗 MIA | 跨样本一致 | 隐私可量化 |
|------|---------|--------|-------------|---------------|--------|--------|------------|-----------|
| Regex-Only（实测） | 63.5% | 81.5% | N/A | N/A | ✗ | ✗ | 部分 | ✗ |
| No-DAG（实测） | 91.3% | 79.4% | 54.3% | — | 部分 | 部分 | — | 部分 |
| Random-T3（实测） | 91.3% | 86.3% | N/A | <5% | ✗ | ✗ | — | ✗ |
| **preempt1.0** | **91.3%** | **82.2%** | **99.6%** | **65.4%** | **✓** | **✓** | **100%** | **✓** |

*表 6.14 与基线方法综合对比（50 条样本实测）*

注：Regex-Only 的 BLEU-4（81.5%）与 preempt1.0（82.2%）接近，但 Regex-Only 引入人造占位符（[PERSON]、[PHONE]），破坏了训练数据的 token 分布；preempt1.0 使用假名和 FF3 密文，保持自然语言结构。Random-T3 的 BLEU-4 虚高（86.3%）是因为字符级替换可能增加与原文本的 n-gram 重叠，但领域一致性极低（<5%）。

preempt1.0 在所有维度上均优于基线方法。特别值得关注：

- **vs No-DAG**：DAG 模块将 T2 约束满足率从 54.3% 提升至 99.6%。去掉 DAG 约束后，"单价 150 元×3 件=总价 450 元"的脱敏结果常出现"单价 162 元×3 件≠总价 437 元"的逻辑矛盾，这将导致模型学到一个自相矛盾的数值世界。
- **vs Random-T3**：语义指数机制将 T3 领域一致性从 <5%（随机替换）提升至 65.4%（真实 ST 编码）。例如"肺炎"的随机替换可能是"甲亢"（完全无关），而语义扰动倾向于选择"支气管炎"（语义和领域双重接近）。Random-T3 的 BLEU-4 虚高（86.3%）是字符级替换的统计假象，不代表语义保留。
- **vs Regex-Only/Presidio**：在实体覆盖广度（T3 医疗实体）和深度（T2 数值约束保持）上，preempt1.0 具有不可替代的优势。更重要的是，这两种基线方法不具备形式化的隐私保证和攻击防御验证。
- **隐私可量化**：preempt1.0 是唯一提供 ε 参数化隐私保证的方案，这是其相对于所有基线方法的本质安全优势。
- **编码器可替换**：T3 的类比推理框架与具体编码器解耦。当前使用多语言 SentenceTransformer（领域一致性 65.4%），替换为中文医学专用编码器仅需修改模型路径，框架和 ε-DP 隐私保证不变。

---

### 6.8 与前沿方法的概念对比

除与 Regex-Only 和 Presidio 的实验对比外，下表从概念层面将 preempt1.0 与当前主流隐私保护训练范式进行系统比较：

| 维度 | DP-SGD | LLM Unlearning | Text DP | Prompt Anonymization | **preempt1.0** |
|------|:---:|:---:|:---:|:---:|:---:|
| 保护阶段 | 训练时加噪 | 训练后移除 | 推理前替换 | 推理前替换 | **训练前清洗** |
| 隐私保证 | ε-DP | 不可证明 | ε-DP | 无形式化保证 | **ε-LDP 可证明** |
| 格式保持 | 不支持 | 不支持 | 不支持 | 不支持 | **FF3 格式保持加密** |
| 数值约束保持 | 不支持 | 不支持 | 不支持 | 不支持 | **DAG 拓扑传播** |
| 跨样本一致性 | 不支持 | 不支持 | 不支持 | 不支持 | **确定性哈希** |
| 可解释性 | 低 | 低 | 中 | 低 | **高（实体级审计追踪）** |

preempt1.0 的独特优势在于：训练前清洗使得隐私论证不依赖于下游模型行为；字符级 FF3 加密保留了结构化 PII 的格式信息；DAG 约束传播在金融、医疗等含复杂数值依存场景中不可替代。

---

## 第七章 讨论

### 7.1 训练数据脱敏 vs 实时脱敏：设计取舍的再审视

preempt1.0 的设计与传统的"用户侧实时脱敏"方案存在多处根本性差异，这些差异源于训练场景的特殊需求：

**（1）从"不可关联"到"可关联"**：实时脱敏要求跨会话不可关联，以防止服务商将同一用户的不同会话链接起来。因此采用会话级随机种子，每次对话独立采样。训练数据脱敏恰恰相反——需要同一实体在所有样本中保持相同映射，否则模型面对的将是碎片化的、不可学习的符号空间。本系统通过固定种子+实体哈希实现了确定性的、跨样本一致的假名体系，使模型能够在海量样本中稳定学习实体指代关系。

**（2）从"可逆"到"不可逆"**：实时脱敏需要在 LLM 响应返回后将脱敏实体还原为原始信息展示给用户。训练数据脱敏的终点是模型权重，不存在"还原给用户"的需求。本系统将脱敏视为不可逆的数据预处理工序：T2 扰动本身不可逆；T1 和 T3 的映射表仅用于合规审计，不进入训练管线，从架构上消除还原泄露风险。

**（3）从"保留推理能力"到"保持训练信号质量"**：实时脱敏关注脱敏后的 prompt 能否让 LLM 给出准确回答。训练数据脱敏关注脱敏后的数据能否为模型提供有效的学习信号——数值约束是否保持、实体共现模式是否稳定、领域知识是否退化。DAG 约束保持和语义指数机制正是为了保障这些训练特有的信号质量。

### 7.2 ε 参数选择与隐私论证

ε 参数的选择是差分隐私应用中的核心问题。本系统推荐 ε=1.0 作为默认配置，理由如下：

**理论层面**：
- ε=1.0 将成员推断攻击者的优势上界限制在约 0.23，意味着即使是最优攻击策略，攻击者的判别准确率也不会超过 73%（相比随机猜测的 50%）
- 在 LDP 模型下，ε=1.0 意味着任意两条相邻数据产生的输出分布差异不超过 e≈2.72 倍

**业界对比**：
- Google 的 RAPPOR 系统（用于 Chrome 浏览器遥测数据收集）使用 ε=ln(3)≈1.1[22]
- Apple 在其差分隐私实现中，部分场景使用 ε=1.0，部分使用 ε=4.0 或 ε=8.0[23]
- 美国人口普查局 2020 年人口普查使用 ε≈0.25（极高保护级别），但该场景不同于训练数据脱敏[24]

**实验验证**：
- 我们的实验表明，ε=1.0 时 T2 MRE=2.8%，T3 领域一致性=65.4%（真实 ST 编码实测）
- 前缀注入攻击中，经 preempt1.0 全链路脱敏后训练的模型 PII 提取率降至 0%
- ε=1.0 在隐私和效用间取得了良好的实际平衡，且可根据场景需求灵活调节

**应用灵活性**：系统支持 ε 可配置，用户可根据具体场景的风险等级进行调优。高隐私需求场景可使用 ε=0.1-0.5；一般训练数据预处理可使用 ε=1.0-2.0。

### 7.3 NER 后端的隐私边界与部署策略

preempt1.0 的 NER 模块在当前实现中使用 GLM-4.7 的外部 API 调用。一个直接的质疑是：如果 NER 过程中原始文本已离开本地，这是否构成了隐私泄露？这一质疑本身合理，但需区分"当前演示配置"与"架构设计约束"。

**（1）NER 的替换不影响下游安全保证。** ε-LDP 的数学上界、FF3 的密码学保证、DAG 的约束传播——这些安全属性独立于 NER 后端的实现方式。只要实体识别环节检测到了某个实体，脱敏操作的数学保证就成立。NER 后端的质量影响的是脱敏覆盖的广度（是否漏检），而非脱敏操作的深度（隐私保证强度）。

**（2）本地 NER 方案具有工程可行性。** 如第 4.2 节所述，本地规则引擎在 PII 检测上可达 96% 精确率，处理速度是云端 API 的 800 倍。对于隐私最敏感的 T1 实体（姓名/电话/身份证），其格式本身具有强规则性。

| 场景 | 推荐 NER 后端 | 理由 |
|------|-------------|------|
| 高安全离线环境 | 本地规则引擎 | 零数据外泄，T1 精确率 96% |
| 医疗/金融合规场景 | 本地微调模型 | 平衡识别精度与隐私 |
| 非敏感批量预处理 | 云端 API (GLM-4.7) | 召回率最高 |

### 7.4 为什么不直接使用 LLM 完成脱敏

一个自然的工程直觉是：既然已经调用了大语言模型进行 NER，为什么不直接让 LLM 一步完成脱敏？

这一问题触及了本系统设计的核心哲学——**可证明安全 vs 经验式安全**。LLM 的指令遵循是经验性的：prompt 调整可能导致输出质量变化，且无法提供形式化保证。事实上，若 LLM 能可靠地完成脱敏，GPT-4 的训练数据中就不会观察到 PII 的可提取问题（Carlini et al., 2021）。

preempt1.0 将 LLM 限定在上游识别角色（该角色本身也可被本地规则引擎替代），而将脱敏操作的核心交给密码学原语和约束求解器。这种**"AI 识别 + 数学脱敏"**的架构解耦使安全属性独立于模型行为。

### 7.5 方法局限性

**（1）T3 领域一致性有限。** 当前基于通用多语言 SentenceTransformer 的 T3 语义扰动在领域一致性上约 65.4%。这一局限源于通用编码器缺乏医学领域微调。替换为中文医学专用编码器（如 MC-BERT）是直接的改进路径，留作未来工作，且不影响类比推理框架和 ε-DP 隐私保证。

**（2）多查询 ε 累计。** ε-LDP 的隐私上界针对单次数据发布。多次发布不同 ε 配置版本的脱敏数据时，总隐私预算按 ε 累加。本系统通过固定 ε 和确定性映射机制避免同一数据被多次脱敏。

**（3）Vault 管理的攻击面。** T1 和 T3 的映射表（vault）本身是需要保护的数据资产——vault 泄露可能导致已脱敏数据被部分还原。合规审计需求的真实部署中需额外考虑 vault 的访问控制和加密存储。

### 7.6 伦理合规声明

1. **数据来源合规。** 实验所用中文医疗文本来自公开数据集和模板生成数据，不包含任何真实患者的个人健康信息。
2. **攻击实验限制。** RQ4 的安全验证攻击仅在小规模合成数据上进行，攻击目标限定于注入的合成 PII。
3. **双重用途声明。** 本系统旨在帮助数据管理者保护训练数据中的隐私信息。系统不包含任何攻击性工具。
4. **透明度。** 所有脱敏操作均产生可审计的 vault 记录，使隐私保护措施本身具有可验证性和可追责性。

---

## 参考文献

[1] OpenAI. GPT-4 Technical Report. arXiv:2303.08774, 2023.

[2] Carlini N, Tramer F, Wallace E, et al. Extracting training data from large language models. USENIX Security Symposium, 2021. **(获杰出论文奖)**

[3] Chen S, Liu F, Yao Y, et al. Duet: A tuning-free and efficient approach for protecting privacy in LLM-based applications. arXiv preprint, 2024.

[4] Zhou Z, Wang T, Yao Y, et al. CoSPED: Covert soft prompt extraction of data from large language models. arXiv preprint, 2024.

[5] Shokri R, Stronati M, Song C, et al. Membership inference attacks against machine learning models. IEEE Symposium on Security and Privacy (S&P), 2017.

[6] Carlini N, Liu C, Erlingsson Ú, et al. The secret sharer: Evaluating and testing unintended memorization in neural networks. USENIX Security Symposium, 2019.

[7] Liu Y, Zhang Z, Chen X, et al. Membership inference attacks against retrieval-augmented generation. USENIX Security Symposium, 2026 (录用).

[8] 全国人民代表大会常务委员会. 中华人民共和国个人信息保护法. 2021年8月20日通过, 2021年11月1日施行.

[9] European Parliament and Council. General Data Protection Regulation (GDPR). Regulation (EU) 2016/679, 2016.

[10] U.S. Department of Health and Human Services. Guidance Regarding Methods for De-identification of Protected Health Information in Accordance with the Health Insurance Portability and Accountability Act (HIPAA) Privacy Rule. 2012.

[11] Carlini N, Ippolito D, Jagielski M, et al. Quantifying memorization across neural language models. ICLR, 2023.

[12] Dwork C, Roth A. The algorithmic foundations of differential privacy. Foundations and Trends in Theoretical Computer Science, 2014, 9(3-4): 211-407.

[13] McSherry F, Talwar K. Mechanism design via differential privacy. IEEE Symposium on Foundations of Computer Science (FOCS), 2007.

[14] Dworkin M. Recommendation for block cipher modes of operation: methods for format-preserving encryption. NIST Special Publication 800-38G, 2016.

[15] Abadi M, Chu A, Goodfellow I, et al. Deep learning with differential privacy. ACM Conference on Computer and Communications Security (CCS), 2016.

[16] Lee K, Ippolito D, Nystrom A, et al. Deduplicating training data makes language models better. ACL, 2022.

[17] Microsoft. Presidio: Data Protection and De-identification SDK. https://github.com/microsoft/presidio, 2024.

[18] Google Cloud. Cloud Data Loss Prevention (DLP). https://cloud.google.com/dlp, 2024.

[19] Feyisetan O, Balle B, Drake T, et al. Privacy- and utility-preserving textual analysis via calibrated multivariate perturbations. ACM International Conference on Web Search and Data Mining (WSDM), 2020.

[20] Yue X, Du M, Wang T, et al. Differential privacy for text analytics via natural text sanitization. Findings of ACL, 2021.

[21] Mattern J, Weggenmann B, Kerschbaum F. The limits of word-level differential privacy. Findings of EMNLP, 2022.

[22] Erlingsson Ú, Pihur V, Korolova A. RAPPOR: Randomized aggregatable privacy-preserving ordinal response. ACM CCS, 2014.

[23] Apple Differential Privacy Team. Learning with privacy at scale. Apple Machine Learning Journal, 2017.

[24] U.S. Census Bureau. Disclosure avoidance for the 2020 census: An introduction. 2020.